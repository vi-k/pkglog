name: pkglog
description: A logger designed specifically for Dart packages.
version: 0.1.1
repository: https://github.com/vi-k/pkglog

topics:
  - debugging
  - logging

environment:
  sdk: ^3.1.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.24.0
